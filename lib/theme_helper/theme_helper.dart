import 'package:flutter/material.dart';

abstract class ThemeHelper {
  // Cores do tinder
  static const pink = Color(0xFFE94976);
  static const orangeAccent = Color(0xFFEE7462);
  static const neutral200 = Color(0xFFFEFFFF);

  // cores do app money
  static const blueAccent = Color(0xFF5E5CE5);
  static const gray = Color(0xFF7C7C7E);
}
