import 'package:flutter/material.dart';

class YourMoneyMock extends StatelessWidget {
  const YourMoneyMock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Container());
  }
}
